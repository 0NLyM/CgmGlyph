# Glyph tables are plain Kotlin objects referenced directly by name from consumer code, so
# nothing here needs a keep rule under the project's current (minifyEnabled false) setup.
# Kept as a file so the module can grow rules without touching consumers' build files.
